# neovim config

---

## attempting to create custom plugins

- [ ] picker (like telescope, but just files & grep with fuzzy search builtin)
    + [ ] use neovim start directory as root
- [ ] completion engine (simple like mini.completion, extended config like nvim-cmp)
